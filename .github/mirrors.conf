# <remote-url> <local-branch>
# One line per mirror. Add a line and it will be created (if the local
# branch doesn't exist yet) and kept in sync twice a day. The remote's
# default branch (HEAD) is what gets pushed to <local-branch>.
https://git.kernel.org/pub/scm/linux/kernel/git/bpf/bpf-next.git bpf-next
https://git.kernel.org/pub/scm/linux/kernel/git/bpf/bpf.git bpf
